# Production environment configuration for OpenShift
# This will be overridden by the deployment environment variable
# The deployment will inject the actual backend route URL
REACT_APP_API_URL=http://localhost:5001